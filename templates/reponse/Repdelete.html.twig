<form method="post" action="{{ path('Repdelete', {'id': reponse.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ reponse.id) }}">
    <center><button class="btn">Supprimer</button></center>
</form>
 